#!/bin/bash
img=$1

[[ -z $img || $img == dev ]] && {
    podman build -f Containerfile.dev -t jappavoo/csw-dev:nerctest-current .
}

[[ -z $img || $img == run ]] && {
    podman build -f Containerfile.run -t jappavoo/csw-run:nerctest-current .
}
